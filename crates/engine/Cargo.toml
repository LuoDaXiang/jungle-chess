[package]
name = "jungle-engine"
description = "斗兽棋规则引擎。唯一规则实现，同时编译为原生（给 AI）和 WASM（给界面）。"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
serde-big-array = "0.5"

[lints.rust]
unsafe_code = "forbid"
